server:
  port: 8022
  servlet:
    context-path: /csp/actuator
spring:
  application:
    name: csp-actuator-server
  profiles:
    active: kafka

logging:
  level:
    org.apache.kafka: warn

# kms地址
kms:
#  address: default
  address: "http://172.20.88.229:8007"
  secret: 0D9Qe30GIMm6oYTcDTuRQgRy7G7R6AMm

# 数据中心地址
data:
  center:
    id: default1
    name: default1

# 定时上报为启动之后的10min为第一次，后续每30min上报一次
node:
  report:
    initialDelay: 10
    fixedDelay: 30

# Topic消息数据时效性: 10min
msg:
  effective:
    time: 10