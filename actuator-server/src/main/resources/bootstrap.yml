server:
  servlet:
    context-path: /csp/actuator
spring:
  application:
    name: csp-actuator-server
  profiles:
    active: dev

logging:
  level:
    org.apache.kafka: warn

data:
  center-id: 0018-0023